# Project Instructions

- In this repository, after any requested code change, run the relevant tests, commit the change to git, and push it to the configured remote.
- Stage only files that belong to the current task. Do not include unrelated dirty working tree changes from earlier sessions.
- If no git remote is configured, create the local commit and report that push is blocked until a remote is added.

